# "Exec dispatch stripped" is a hypothesis, not a verified negative — F9 could never have fired, F5 needs a CheatManager, F6 hits a setter the game's camera can mask

Filed by: `/gr`, 2026-09-04
Supersedes: ENGINE-DOSSIER.md §7 "⇒ THE COHERENT READING: this build kept its input/action bindings and STRIPPED ITS EXEC DISPATCH" and its `[verified-live 2026-09-03, n=3 commands]` tag; also §7 "The debug camera is unreachable by ANY input route" insofar as it rests on that reading
Topic: `external-research/topics/2026-09-04-the-exec-channel-was-never-proven-dead-each-negative-has-a-cheaper-cause.md`
Dossier sections: §7 (console / exec channel); the board's two `[PD]` in-process rows

- **F9=`shot` is bound only inside `[NTGameFramework.NTCam_DebugInput]`** (live ini, line 143, section 114–169) — the debug camera controller's own input class, which does not exist in normal play. The test could not have produced a positive; "F9 does not depend on our edit" was true but irrelevant. `[inferred-static 2026-09-04, read from the shipped config]`
- **F5=`ToggleDebugCamera` is a `GameFramework.GameCheatManager` exec**, and a CheatManager exists only if `PlayerController.AddCheats()` ran, gated on `GameInfo.AllowCheats()`; the `EnableCheats` exec that forces it is FINAL_RELEASE-guarded. The bound-command dispatch chain (`UnPlayer.cpp:2923–2954`) tries PlayerInput → PlayerController → Pawn → InvManager → Weapon → HUD → GameInfo → CheatManager-if-non-null and returns false silently when no object owns the name — with dispatch fully alive. The pad chord ends at the same missing object. `[inferred-static 2026-09-04, public UE3 source]`
- **F6=`FOV 120` → `PlayerController.FOV` → `PlayerCamera.SetFOV`**, a one-shot the game's own chase camera (`MonkeyChaseCamera.ini`) can overwrite each tick. The community FOV recipe for this game is *that binding* under `[MonkeyGame.MKInput]` (§10a), reported working. `[hypothesis]`
- **Section placement is an open variable:** the test lines went into `[Engine.PlayerInput]`; the community uses `[MonkeyGame.MKInput]`, a section present in the game-folder ini (line 389) but absent from the Documents copy the game loads.
- **One-launch `[FLAT]` re-test that can settle it:** bind `Pause` (unguarded `PlayerController` exec, no CheatManager, world visibly freezes) in **both** sections, plus the community's `FOV 0` line verbatim. Pauses → §7 `[disproved]` and both in-process rows become "construct a CheatManager" (see the companion drop). Does not pause in either section → the first *valid* negative for the stripped-dispatch reading.

Suggested dossier change: retag the §7 "STRIPPED ITS EXEC DISPATCH" reading `[hypothesis]`, record why each of the three negatives is non-diagnostic (F9 section, F5 owner object, F6 masking), add the `Pause`-in-both-sections re-test as the `[FLAT]` row, and reword "unreachable by ANY input route" to "unreachable by the routes tried, none of which is yet a valid negative". Keep the six-key budget: this is one key.
